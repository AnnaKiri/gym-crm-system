spring:
  config:
    activate:
      on-profile: component-test
eureka:
  client:
    enabled: false
keycloak:
  url: http://localhost
  realm: gym-crm-system-realm
de:
  flapdoodle:
    mongodb:
      embedded:
        version: 4.4.10