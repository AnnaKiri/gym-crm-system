spring:
  datasource:
    password: local_password
  security:
    oauth2:
      client:
        registration:
          keycloak:
            client-id: client-id
            client-secret: client-secret
springdoc:
  swagger-ui:
    oauth:
      client-id: client-id
      client-secret: client-secret